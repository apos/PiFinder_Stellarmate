# Screenshots for Readme_PFinder_LX200.md

This directory is referenced by the `📷 Screenshot: ...` placeholders in
[`../../../Readme_PFinder_LX200.md`](../../../Readme_PFinder_LX200.md).

Add captures here and replace the corresponding placeholder block in that document with:

```markdown
![Description](docs/images/pfinder_lx200/your_filename.png)
```

Suggested screenshots still needed (see the placeholders in context for exactly what each should
show):

- [ ] StellarMate Web Manager — equipment profile with PiFinder LX200 / mount driver / PiFinder
      Mount Bridge listed
- [ ] INDI Control Panel — the three device tabs side by side
- [ ] INDI Control Panel — "PiFinder Mount Bridge" tab with Coupling/Manual/Drift controls visible
- [ ] KStars sky map — right-click context menu on a star showing both mount and PiFinder LX200
      as GoTo targets
- [ ] SkySafari — telescope connection dialog (port 9624) and sky view with PiFinder's position
      marker
